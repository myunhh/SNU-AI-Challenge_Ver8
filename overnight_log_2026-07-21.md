# 야간 자동 진행 기록 — 2026-07-21 밤

사용자 취침 중 자동 모니터링/문제 해결 기록. 아침에 이 파일 + 아래 요약 참고.

## 대기열

1. **TTA24 (완전균형 24뷰, S4 전체) — test 819건** — 진행 중 (시작 16:50경, 예상 완료 ~21:00경)
   - `runs/test_champ_tta24/submission.csv`
2. **캐스케이드(2단계 pairwise) test-probe — test 819건** — TTA24 완료 후 자동 시작 예약
   - baseline은 기존 `runs/test_champ_tta8b/submission.csv`(현 챔피언 레시피, LB 0.93019 실측) 재사용
   - `runs/test_champ_tta8b_cascade/submission.csv` 생성 후 grade.py(참고용) + score_agreement.py(champion 대비 실질 비교)로 평가
   - **LB 제출은 안 함** — 파일만 준비, 제출 여부는 사용자 판단

## 이벤트 로그

- 17:23 — TTA24 13% 진행, 캐스케이드 test-probe 대기열 정상 예약 확인. GPU 100% 가동, 이상 없음.
- 22:16 — **TTA24 완료** (819건, 에러 0). `runs/test_champ_tta24/submission.csv`.
  - grade.py(참고용, 자기참조 편향): EM 0.9365 — 실제 신뢰 불가, 무시할 것
  - **score_agreement.py vs 현 챔피언(TTA8-balanced, LB 0.93019 실측)**: EM 일치율 **0.9805**
    (819건 중 약 16건만 다름), 쌍순서 일치율 0.9933 — TTA8과 매우 유사. TTA24가 TTA8보다
    나은지는 로컬로 판단 불가(라벨 없음), LB 제출 전엔 모름.
  - 체인 정상 작동 확인 — TTA24 완료 즉시(같은 초) 캐스케이드 test-probe 자동 시작됨,
    GPU 유휴 없이 넘어감. (이전 DPO 체인 때의 "조용히 죽는" 문제 이번엔 재발 안 함 —
    파일 기반 스크립트로 바꾼 게 효과 있었던 듯)
- 22:17 — 캐스케이드(tau=0.15) test-probe 시작, GPU 100%, 정상 진행 중.
- 00:06 — **캐스케이드 test-probe 완료** (819건, escalated=7 — tau=0.15에서 이 조합은
  819건 중 딱 7건만 애매하다고 판단해 pairwise 재검증을 발동함).
  - **버그 발견·수정**: `run_test_cascade_probe.sh`의 `score_agreement.py` 호출에
    `--out` 인자가 빠져 비교 단계가 에러로 스킵됨(전에 제가 직접 고쳤던 것과 같은
    실수를 스크립트에도 그대로 남겨뒀었음) — 수동으로 재실행해 결과 확보, 스크립트도
    수정해둠(다음에 재사용해도 안전).
  - grade.py(참고용, 자기참조 편향): EM 0.9365 — 무시할 것
  - **score_agreement.py vs 챔피언(TTA8-balanced)**: EM 일치율 0.9768 (819건 중 19건 다름)
  - **교차분석**(직접 계산): 19건 중 **4건만 실제 캐스케이드 발동(escalated)으로 바뀜**,
    나머지 **15건은 escalated 아닌데도 baseline과 다름** — 즉 같은 설정(`--tta 8
    --tta-balanced8`, cascade 없이 계산되는 부분)인데도 두 별개 실행 사이에 819건 중
    15건(1.8%)이 그냥 달라짐. score24는 순수 forward pass라 원칙적으로 결정적이어야
    하는데 실측은 아님 — GPU 커널(sdpa attention 등) 비결정성으로 추정, 확정 원인 규명은
    안 함(아침에 필요하면 추가 조사).
  - **한계**: test는 라벨이 없어 이 4건의 flip이 정답에 가까워진 건지 아닌지 로컬로는
    전혀 알 수 없음 — LB 제출 없이는 캐스케이드가 도움이 됐는지 확인 불가.
  - **결론(잠정)**: tau=0.15에서 캐스케이드가 819건 중 겨우 7건에만 개입 — 설령 전부
    도움이 되는 방향이어도 LB 최대 이동폭이 매우 작음(~0.85pp 상한, 실제는 더 작을
    가능성 높음). 지금 레시피에선 이 tau로는 기대값이 낮아 보임 — LB 제출은 보류함
    (사용자 판단 필요, 하루 2회 제한 고려 시 우선순위 낮아 보임).
  - 산출물: `runs/test_champ_tta8b_cascade/submission.csv`,
    `runs/test_champ_tta8b_cascade/agreement_vs_tta8b.json`

## 00:07 현재 상태 — 대기열 전부 소진, GPU 유휴

예약했던 두 작업(TTA24, 캐스케이드 probe) 모두 정상 완료. GPU 0% 유휴, 걸린 프로세스 없음.
오늘 밤 몇 번 "이미 해봤음/방향이 문서와 안 맞음"으로 계획이 틀어진 적이 있어서(BALANCED8
코셋 불일치, DPO 이어학습 취소 등), 다음 실험은 추측으로 더 벌이지 않고 **사용자 지시
대기 중**입니다 — GPU를 굳이 새 실험으로 채우진 않았습니다(방향 잘못 잡고 몇 시간 태우는
것보다 유휴가 안전하다고 판단).

**아침에 검토할 것 / 선택지**
- TTA24가 챔피언(TTA8-balanced) 대비 겨우 16건 다름 — LB 제출해서 실제로 더 나은지
  확인해볼 가치는 있음(단, 하루 2회 제한 고려).
- 캐스케이드는 tau=0.15에서 819건 중 7건만 발동 — 이 tau로는 기대 효과가 작아 보임.
  더 큰 tau로 재시도하거나, 아니면 이 레버는 접고 다른 걸 볼지 판단 필요.
- 둘 다 제출 안 하고 그대로 둘 수도 있음.
