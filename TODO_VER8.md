# TODO_VER8 — 개선 계획 (2026-07-12 작성)

> **⚠️ 이 문서는 `Ver8/TODO.md`로 승계·대체됨** (07-12 저녁). P0·P1·P3 실측 기록은
> 여기 보존, 신규 작업은 Ver8에서. P4-a(SigLIP2 힌트)는 앙상블 금지 규정 재확인으로
> Ver8에서 폐기됨.

**출발점 (07-12 저녁 갱신)**: 현 최고·제출 구성 = **Ver3+TTA3 bf16, LB 0.865**
(LB_LOG #3 — 4bit 서빙 페널티 ~4pp 판명으로 v7 0.85863에서 복귀).
**모든 제출은 bf16 서빙.** 홀드아웃 쌍순서 +2.6~2.7pp = LB 예측(bf16 한정).
마감 07-24 23:59 → 남은 12일, LB 슬롯 최대 24회.

**이번에 배운 것 (계획의 근거)**:
- 홀드아웃(945건)이 LB +3.49pp짜리 이득을 완전히 놓쳤다(동률로 기각했었음).
  원인 후보: (a) 945건이 Ver1부터 모든 결정에 재사용된 적응적 과적합,
  (b) train/test 분포 차이. → **검증 체계 수선이 다른 모든 실험의 전제**
- LB 스케일은 EM(0.55대)이 아니라 쌍순서(1−KT/6, 0.83대)와 유사 —
  **LB가 부분점수 채점일 가능성 높음** (v3 4bit: 홀드아웃 쌍순서 0.8351 ↔ LB 0.82373)
- 누적 교훈 유지: 새 정보원 없는 재질의/재표현은 EM을 못 움직인다(Ver5·Ver7 홀드아웃).
  단, **지표·검증·후처리·앙상블 축은 아직 미개척**이고 전부 무학습·저비용.

---

## P0. 검증 체계 수선 — 2026-07-12 완료 (규정 확인만 잔여)

- [ ] **LB 채점 방식 규정 확인** (사용자): 대회 페이지에서 부분점수 여부·산식 확인.
  ※ 드리프트 포렌식이 **쌍순서(1−KT/6) 계열임을 강하게 시사** — 두 제출의 LB
  점수차(3.49pp)가 쌍순서 기준 이론 상한(3.54pp)의 98.6%로 포화 + 스케일 정합.
  규정으로 최종 확정만 남음 (`runs/drift_forensics_v7.md` §2)
- [x] `perm.py`에 `pairwise_score`/`position_score` 추가(SSOT), `eval_report.py`
  리포트에 병기, `ab_gate.py`에 `--metric pairwise|position` + truth 소급 경로.
  테스트 54개 통과. 과거 런 11개 소급 재계산 완료 — EM 순위와 쌍순서 순위가
  대체로 일치해 **과거 기각 결정 중 지표 때문에 잘못 버린 후보는 없음** 확인
- [x] ~~홀드아웃 재분할~~ → 불가능(어댑터가 홀드아웃 외 전체로 학습). 대체한
  **드리프트 포렌식 완료** (`runs/drift_forensics_v7.md`): ① test가 홀드아웃보다
  훨씬 쉬움(margin 중앙값 0.6 → 0.99) — 가설 (b) 분포 차이 실증, ② v3↔v7
  불일치 ~111건에서 v7이 사실상 전부 정답 쪽(포화 논증) — v7의 test 우위는
  체계적, ③ 항등예측률 정상(규약 문제 없음)
- [x] `LB_LOG.md` 생성(엔트리 2개 + 관찰 기록). 프로토콜 수정: 홀드아웃 ±1pp
  동률 후보는 LB 슬롯으로 최종 판정, 쌍순서 게이트(`gate_v7_tta3_pairwise.json`:
  홀드아웃 쌍순서 v7 −0.74pp인데 LB +3.49pp — 괴리의 공식 기록) 병행

## P1. metric-optimal decoding — 2026-07-12 실측 ❌ 기각

LB가 부분점수라면 argmax(최빈 순열) 대신 기대 쌍순서 점수를 최대화하는 순열을
내는 게 이론상 최적 — 이라는 가설이었으나 실측 기각.

- [x] `predict.py`에 `scores24` 덤프 추가(하위호환, 이후 모든 런에 자동 포함),
  `scripts/decode_expected.py` 신규(+테스트 3개, 총 55개 통과)
- [x] 홀드아웃 재실행(`cal_v7_ckpt1800_tta3_scores`, 0.5577 정확 재현) 후 A/B:
  **Δ쌍순서 −0.25pp** (EM −1.3pp), 예측 변경 107건(11.3%)이 순손실
- [x] 온도 보정 프로브(반반 교차검증, T∈[0.25,5]): halfA 전 구간 음수(최선
  T=0.5도 −0.18pp), halfB 검증 +0.14pp는 노이즈 — **어떤 온도로도 이득 없음**

**해석**: 모델의 24-way 분포에서 argmax 밖 확률 질량은 보정된 불확실성이 아니라
노이즈다. 저마진 = 진짜 모호함이라는 Ver5·cascade 계열 결론과 세 번째로 일치
("분포 꼬리를 다르게 읽는" 접근 전반이 이 태스크에선 소진됨). scores24 덤프
인프라는 유지(비용 0, 향후 분석용).

## ~~P2. 인코딩 앙상블 v3 ⊕ v7~~ — 제외 (2026-07-12, 사용자 결정)

앙상블은 진행하지 않기로 함. (참고 기록: 두 어댑터 예측 일치율 59.7% —
방향 자체는 유효해 보였으나 제외. 점수 dump 인프라는 P1이 어차피 만든다)

## P3. 현 제출 구성 다듬기 — 2026-07-12 v7분 완료, v3-TTA5만 잔여

- [x] **TTA5 재게이트 (v7-1800)**: EM −0.32pp / 쌍순서 +0.11pp, CI 0 포함 →
  ❌ 기각. TTA 이득은 3뷰에서 포화 (`runs/gate_v7_tta5_*.json`)
- [x] **4bit 서빙 재게이트 (v7-1800)**: EM −1.59pp / 쌍순서 −0.76pp → ❌ 기각.
  LB 실측(4bit test 페널티 ~4pp, LB_LOG #3)과 방향 일치 — **bf16 원칙 확정**
- [x] **TTA5 재게이트 (v3, LB 선두 계열)**: EM 0.5608(+0.42pp) / 쌍순서
  0.8397(+0.11pp), CI 0 포함 → ❌ 기각 (`runs/gate_v3_tta5_*.json`).
  **양 계열 모두 TTA는 3뷰 포화 확정** — P3 종결. 예측 LB로도 0.866대라
  슬롯 소모 가치 없음

## P4. 백로그 (P0~P2 소진 후 or 병렬 여유 시)

- [ ] SigLIP2 등 **별도 모델 임베딩 힌트** — 저마진 구간 구제의 유일한 남은 경로
  (Ver5 결론). 설계 비용이 커서 P1/P2 결과 본 뒤 착수 판단
- [ ] video_dup 레시피 재학습 변형(색상 증강 등) — 6.5h/회라 후순위

## 마감 전 필수 (기존 리마인더 승계)

- [ ] 오프라인 리허설: `HF_HUB_OFFLINE=1`로 test 819 풀런 + 3090 환산 시간
  (**현 제출 구성 = v7 ckpt1800+TTA3 bf16 기준으로**)
- [ ] 본선 검증자료: 게이트 기록·audit 덤프·포렌식 리포트·LB 대장 보존

## 실행 순서 제안 (12일)

1. **D1~2**: P0 전부 (규정 확인 + 지표 확장 + 드리프트 포렌식) — 이후 판단의 토대
2. **D2~4**: P1 metric-optimal decoding → 이득 있으면 LB 검증
3. **D4~**: P3 소항목 (TTA5·4bit 재게이트) + P4(SigLIP2) 착수 판단
4. **D10~**: 오프라인 리허설 + 검증자료 정리 (신규 실험 동결)
