# GPU 전용 (3090 / VESSL) — 학습·양자화 추론용
# ⚠️ 3090과 VESSL의 버전을 반드시 일치시킬 것 (노션 결정사항)
-r requirements.txt
bitsandbytes>=0.45.0
torchvision               # Qwen3-VL AutoProcessor의 video sub-processor가 로딩 시점에 요구
# flash-attn                 # 선택: 빌드 실패 시 SDPA로 자동 폴백 (engine.py)
# unsloth                    # 선택: 학습 가속 (train_sft.py --unsloth)
# optimum-quanto             # 선택: quantized KV cache (engine.py kv_quant)
# trl                        # 미사용: DPO는 score24 단일토큰 구조를 살려 자체 구현
                             # (train_dpo.py — TRL DPOTrainer의 멀티턴 생성 전제가 불필요)
